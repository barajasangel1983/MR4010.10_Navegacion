# Core numeric / image
numpy>=1.21.0
opencv-python>=4.5.0
Pillow>=9.0.0

# Deep learning (BC + CIL models, TorchScript inference)
torch>=1.12.0
torchvision>=0.13.0

# Controller I/O (PS4 / gamepad support)
pygame>=2.1.0

# Data handling
pandas>=1.3.0
scipy>=1.7.0

# Classical ML (SVM pedestrian/vehicle detectors)
scikit-learn>=1.0.0

# Visualization (notebooks)
matplotlib>=3.5.0
seaborn>=0.12.0

# Config
pyyaml>=5.4

# Note: Webots Python bindings (controller, vehicle) are provided by the
# Webots installation — they are not available on PyPI.
